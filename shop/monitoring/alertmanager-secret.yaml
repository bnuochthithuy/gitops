apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-shop-email
  namespace: monitoring
type: Opaque
stringData:
  # Thay YOUR_EMAIL và YOUR_APP_PASSWORD bằng giá trị thực
  # Gmail: bật 2FA → tạo App Password tại https://myaccount.google.com/apppasswords
  smtp_password: "YOUR_APP_PASSWORD"
